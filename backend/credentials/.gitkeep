# Place your GCP service-account JSON key here.
# Example filename: gcp-service-account.json
#
# This file keeps the directory tracked by git while the actual
# JSON key is ignored (see root .gitignore).
